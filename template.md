# 技術教材作成の「秘伝のレシピ」 (Go/CLI/Web対応版)

このテンプレートは、学習者が「本番で使える技術」を最短距離で、かつ深く理解しながら習得するための設計図です。

---

## 1. 全体構成: 3層構造

教材は必ず以下の3層で構成し、各フェーズの目的を明確にする。

### 層1: 基礎編 (training / basics)
- **目的**: 1トピック1ファイルで、基本機能を確実に理解する。
- **番号**: `00_`, `01_` ... と番号を振り、学習順序を明示。
- **構成**: `example/`, `exercise/`, `answer/` の3点セット。

### 層2: 応用編 (advance)
- **目的**: 複数の機能を組み合わせた高度なパターンや、実務での定石を学ぶ。
- **内容**: 並行処理の制御、外部API連携、複雑な構造体設計など。

### 層3: 実践編 (practice / project)
- **目的**: ゼロから一つのツール/アプリを構築し、設計能力を養う。
- **特徴**: 詳細な「設計書（仕様）」のみを提示し、実装コードは提供しない（演習課題）。

---

## 2. README.md の執筆ルール

### 4.1 冒頭: 一文要約
最初の1文で「何を実現するものか」を太字で明確にする。
例: 「このトピックでは、**『プログラム実行時に外側から指示を与える引数（Args）』**を扱う方法を学びます。」

### 4.2 「なぜ必要か」の実務背景
技術解説の前に、具体的な業務シーンを挙げ「これがないと何に困るか」を説明する。

### 4.3 比喩（たとえ話）の導入
1トピックにつき最低1つは、日常の体験に例えた説明を入れる（記憶に定着させるため）。
例: 「Channelは、ゴルーチンという職人の間で部品を渡すための『ベルトコンベア』です。」

### 4.4 行番号付き解説 (★最重要)
コードの解説は、対応する行番号を明記し、最も重要な箇所には「★ここが一番重要」を付ける。
```markdown
## 1. チャネルの作成 (10-12行目)
## 2. データの送信 (15-20行目) ★ここが一番重要
```

### 4.5 3つのTips (GitHubスタイル)
`> [!TIP]` を使い、実務での落とし穴やベストプラクティスを3つ提示する。

---

## 3. コード作成のルール

### 5.1 最小限の原則
- トピックに関係ない複雑なロジックは排除し、1ファイルで完結させる。
- パッケージ競合を防ぐため、サブディレクトリ (`example/`, `exercise/`) を活用する。

### 5.2 実行確認と出力の工夫
- `go run main.go` で即座に動作確認できること。
- 出力ログには `[ProcessName]` のようにプレフィックスを付け、処理の流れを追跡可能にする。

### 5.3 演習 (exercise) の設計
- `main.go` 内に `// 演習1: ...` とコメントを残し、ユーザーが「どこに何を書けばいいか」迷わないようにする。

---

## 4. プロンプト用テンプレート
新しい技術を学ぶ際に、以下のプロンプトをAIに投じることで高品質な教材を出力させる。

> 「あなたは技術教材のプロフェッショナルです。
> `template.md` の設計原則（3層構造、行番号解説、比喩の導入など）に従い、
> 以下のトピックに関する教材を作成してください。
> 
> 対象技術: [技術名]
> 対象読者: [レベル]
> 学習トピック: [トピック一覧]」
