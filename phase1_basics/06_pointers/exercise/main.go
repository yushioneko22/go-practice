package main

func main() {
	// 演習1: int型の変数 `a` を宣言し、そのポインタ `p` を作成してください

	// 演習2: `p` を使って `a` の値を表示してください
	// ヒント: *p

	// 演習3: `p` を使って `a` の値を 50 に書き換えてください

	// 演習4: ポインタ引数を受け取る関数 `isEven` を作成し、中身が偶数なら true, 奇数なら false を返すようにしてください（呼び出し時にアドレスを渡すこと）
}
